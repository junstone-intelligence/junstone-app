/** @type {import('prettier').Config} */
const config = {
  printWidth: 100,
  semi: false,
  tabWidth: 2,
  singleQuote: true,
  arrowParens: 'avoid',
  trailingComma: 'none',
  jsxSingleQuote: false,
  quoteProps: 'consistent'
}

export default config
